defmodule EvisionTest.MixProject do
  use Mix.Project

  def project do
    [
      app: :evision_test,
      version: "0.1.0",
      elixir: "~> 1.13",
      start_permanent: Mix.env() == :prod,
      deps: [{:evision, "~> 0.1.0-dev", path: "../evision"}]
    ]
  end

  def application do
    [extra_applications: [:logger]]
  end
end
