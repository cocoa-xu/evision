defmodule Evision.QtButtonTypes do
  @type enum :: integer()
  @doc enum: true
  def cv_QT_PUSH_BUTTON, do: 0
  @doc enum: true
  def cv_QT_CHECKBOX, do: 1
  @doc enum: true
  def cv_QT_RADIOBOX, do: 2
  @doc enum: true
  def cv_QT_NEW_BUTTONBAR, do: 1024
end
