defmodule Evision.XImgProc.HoughOp do
  @type enum :: integer()
  @doc enum: true
  def cv_FHT_MIN, do: 0
  @doc enum: true
  def cv_FHT_MAX, do: 1
  @doc enum: true
  def cv_FHT_ADD, do: 2
  @doc enum: true
  def cv_FHT_AVE, do: 3
end
